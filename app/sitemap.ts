import type { MetadataRoute } from "next";
import { services, site } from "@/lib/site";
import { projects } from "@/lib/projects";
import { blogPosts, neighborhoods } from "@/lib/content";
export default function sitemap(): MetadataRoute.Sitemap { return ["", "/hizmetler", "/istanbul", "/kurumsal", "/hakkimizda", "/sss", "/teklif-al", "/projeler", "/blog"].map(path => ({ url: `${site.url}${path}`, changeFrequency: path === "/blog" ? "weekly" as const : "monthly" as const, priority: path === "" ? 1 : .7 })).concat(services.map(s => ({ url: `${site.url}/hizmetler/${s.slug}`, lastModified: new Date(s.updatedAt), changeFrequency: "monthly" as const, priority: .8 })),projects.map(project=>({url:`${site.url}/projeler/${project.slug}`,lastModified:new Date(project.updatedAt),changeFrequency:"monthly" as const,priority:.72})),blogPosts.map(post=>({url:`${site.url}/blog/${post.slug}`,lastModified:new Date(post.updatedAt),changeFrequency:"monthly" as const,priority:.76})),neighborhoods.map(item=>({url:`${site.url}/istanbul/${item.districtSlug}/${item.slug}`,lastModified:new Date(item.updatedAt),changeFrequency:"monthly" as const,priority:.74}))); }
