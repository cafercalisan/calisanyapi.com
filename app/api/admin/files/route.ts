import { NextResponse } from "next/server";
import { verifyAdmin } from "@/lib/admin";
export async function GET(request:Request){if(!(await verifyAdmin()))return NextResponse.json({error:"Yetkisiz."},{status:401});const path=new URL(request.url).searchParams.get("path");if(!path||path.includes(".."))return NextResponse.json({error:"Geçersiz dosya."},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.SUPABASE_SERVICE_ROLE_KEY;const bucket=process.env.SUPABASE_STORAGE_BUCKET||"quote-photos";if(!url||!key)return NextResponse.json({error:"Depolama yapılandırılmadı."},{status:503});const r=await fetch(`${url}/storage/v1/object/sign/${bucket}/${path}`,{method:"POST",headers:{apikey:key,Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify({expiresIn:300})});if(!r.ok)return NextResponse.json({error:"Dosya bulunamadı."},{status:404});const data=await r.json();return NextResponse.redirect(`${url}/storage/v1${data.signedURL}`)}
