import type { Metadata } from "next";import Image from "next/image";import Link from "next/link";import { ArrowUpRight } from "lucide-react";import { SiteHeader } from "@/components/SiteHeader";import { SiteFooter } from "@/components/SiteFooter";import { projects } from "@/lib/projects";
export const metadata:Metadata={title:"Yapı Uygulamaları ve Projeler",description:"PVC pencere, cam balkon, sineklik ve tamamlayıcı yapı sistemlerinde ölçüye özel uygulama örneklerini ve çözüm detaylarını inceleyin.",alternates:{canonical:"/projeler"}};
export default function Page(){return <><SiteHeader/><main className="inner-page"><section className="page-hero technical-paper"><p className="kicker">Uygulama arşivi</p><h1 className="font-display">Detayda görülen,<br/><em>ölçüde çözülen.</em></h1><p>Paylaşım izni bulunan uygulama görsellerini sistem, ihtiyaç ve çözüm bilgileriyle birlikte yayınlıyoruz. Arşiv yeni saha kayıtlarıyla düzenli olarak genişletilir.</p></section><section className="project-grid">{projects.map(project=><Link key={project.slug} href={`/projeler/${project.slug}`}><figure><Image src={project.image} alt={project.title} fill sizes="(max-width:900px) 100vw, 33vw"/></figure><p>{project.serviceName} · {project.area}</p><h2 className="font-display">{project.title}</h2><span>Uygulamayı incele <ArrowUpRight size={15}/></span></Link>)}</section></main><SiteFooter/></>}
